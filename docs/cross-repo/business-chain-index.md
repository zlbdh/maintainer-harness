# Business Chain Index

Use this file to describe end-to-end user or maintainer workflows that cross repository boundaries.

Generic example:

```text
web -> api -> worker -> notification
```

For each chain, record:

- trigger
- participating repositories
- source of truth
- downstream consumers
- acceptance checks
- known risks

Keep examples generic unless the repository owner has explicitly approved a public case study.
