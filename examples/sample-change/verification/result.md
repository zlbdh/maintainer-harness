# Verification Result

## Status

Example only.

## Expected Commands

```powershell
.\scripts\checks\validate-repos.ps1
.\scripts\bootstrap\verify-workspace.ps1
```

## Notes

A fresh checkout may warn that sample repositories are not cloned. That warning is expected unless `repos/repos.yaml` has been replaced with real local repository entries.

